## Цель

Расширить библиотеку с 11 до 17 опросников и существенно усилить SEO: добавить кластерные ленды, удлинить контент на страницах тестов, выпустить связанные статьи в блоге.

## 1. Шесть новых опросников (только публичные/валидированные русские версии)

Все шкалы — в открытом доступе, разрешены к свободному использованию для скрининга.

| Код | Название | Кластер | N вопр. | Источник |
|---|---|---|---|---|
| PCL-5 | Чек-лист ПТСР по DSM-5 | trauma (новый) | 20 | National Center for PTSD, public domain |
| ISI | Индекс тяжести бессонницы | sleep (новый) | 7 | Morin, открытая русская валидация |
| AUDIT | Тест на употребление алкоголя | addiction (новый) | 10 | ВОЗ, public domain |
| ECR-R-12 | Стили привязанности (краткая) | relationships (новый) | 12 | Fraley/Wei, открытая |
| BFI-10 | Большая пятёрка, краткая | personality (новый) | 10 | Rammstedt & John, открытая |
| DERS-16 | Шкала трудностей эмоц. регуляции | cbt-tools | 16 | Bjureberg, открытая |

Файлы: по одному `src/data/tests/{code}.ts` по образцу существующих (`phq9.ts`), регистрация в `src/data/tests/index.ts`. Подшкалы там, где есть (PCL-5: B/C/D/E; ECR-R: тревога/избегание; BFI-10: O/C/E/A/N; DERS-16: 5 факторов).

## 2. Новые кластеры

В `src/pages/TestsHub.tsx` добавить чипы фильтров: `trauma` (Травма/ПТСР), `sleep` (Сон), `addiction` (Зависимости), `relationships` (Отношения), `personality` (Личность). Существующие сохранить.

## 3. SEO-расширение страницы хаба `/tools/tests`

В `TestsHub.tsx`:
- H1 + лид оставить, добавить под фильтрами 5–7 SEO-секций H2 («Тесты на депрессию», «Тесты на тревогу», «Тесты для отношений», «Тесты на ПТСР», «Тесты на бессонницу», «Личностные тесты», «Когда тест — это не диагноз»), каждая с 1–2 абзацами и внутренними ссылками на конкретные тесты и статьи блога.
- Расширенный FAQ-блок (8–10 вопросов) → JSON-LD `FAQPage`.
- Обновить `seoTitle`/`description` под более широкие запросы («психологические тесты онлайн бесплатно», «тест на …»).

## 4. SEO-блоки на каждой странице теста

В `src/components/tests/TestLayout.tsx` после блока «О методике» добавить опциональные секции, читаемые из `TestConfig` (поля добавляются в `types.ts`, опциональны — старые тесты не ломаются):

- `symptoms`: «Когда стоит пройти этот тест» — список симптомов/триггеров.
- `whatNext`: «Что делать с результатом» — 3 сценария по диапазонам.
- `compareWith`: «Чем отличается от похожих шкал» — короткие сравнения с 2–3 родственными тестами + внутренние ссылки.
- `relatedArticles`: ссылки на статьи блога.

Заполнить для 6 новых + дописать для 11 существующих по общему шаблону.

## 5. Контентные статьи в блоге

Шесть якорных статей под информационные запросы, связанные с тестами (создаются через существующий механизм блога):

1. «Как понять, что у меня депрессия» → PHQ-9, BDI-альтернативы.
2. «Тревожность: симптомы, проверка, что делать» → GAD-7.
3. «Что такое ПТСР и как себя проверить» → PCL-5.
4. «Бессонница: как отличить от усталости» → ISI.
5. «Стили привязанности: тревожный, избегающий, надёжный» → ECR-R.
6. «Большая пятёрка vs MBTI: почему психологи не используют MBTI» → BFI-10.

Каждая 800–1200 слов, H2-структура, внутренние ссылки на тест и обратно.

## 6. Технические SEO-задачи

- `public/sitemap.xml`: добавить 6 новых URL тестов + 6 URL статей блога.
- `public/llms.txt` / `llms-full.txt`: добавить новые тесты и статьи.
- Хлебные крошки и `Quiz` schema уже корректно генерируются в `TestLayout` — переиспользуем.
- Добавить `relatedTests` для новых тестов и для существующих, чтобы перелинковка работала в обе стороны.

## Технические детали

- Никаких миграций БД, всё статично.
- Поля в `TestConfig` (`symptoms`, `whatNext`, `compareWith`, `relatedArticles`) — опциональные, обратная совместимость.
- Подшкалы в новых тестах используют существующий `sumByItems` (уже работает в PDF-отчёте и шкале интерпретации).
- Шкала интерпретации (`testRanges.ts`) и PDF (`testReportPdf.ts`) автоматически подхватят новые тесты без изменений.

## Что НЕ входит

- BDI-II, STAI, MBTI — копирайт, не включаем (упоминание в сравнительных блоках допустимо).
- Серверные изменения, авторизация, сохранение результатов.

## Файлы

Новые: 6× `src/data/tests/{code}.ts`, 6× постов блога (через текущую систему).
Изменяются: `src/data/tests/index.ts`, `src/data/tests/types.ts`, `src/pages/TestsHub.tsx`, `src/components/tests/TestLayout.tsx`, существующие `src/data/tests/*.ts` (добавить опциональные SEO-поля), `public/sitemap.xml`, `public/llms.txt`, `public/llms-full.txt`.
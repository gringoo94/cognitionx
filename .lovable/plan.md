

## Оформление страницы блога

### Проблема сборки
Ошибки `tags is missing` — устаревший кеш. Все 17 постов уже содержат поле `tags`. Пересборка должна решить проблему. На всякий случай — при реализации я принудительно пересоберу проект.

### Улучшения страницы /blog

1. **Заголовочный блок** — добавить декоративный badge «Блог» над заголовком (как на главной), подзаголовок с описанием блога
2. **Теги** — увеличить размер badge-кнопок, добавить плавную анимацию при переключении фильтра
3. **Карточки постов** — показывать теги внутри карточек (уже есть), добавить hover-эффект на badge тегов
4. **Пагинация** — перевести "Previous"/"Next" на русский ("Назад"/"Вперёд")
5. **Пустое состояние** — улучшить визуал пустого фильтра (иконка + текст)

### Файлы
- `src/pages/BlogList.tsx` — основные изменения стилей и структуры

